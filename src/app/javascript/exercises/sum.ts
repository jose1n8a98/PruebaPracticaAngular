export const input = [2, 10, 40, -1, 20, 33, 60, 100, 44, 75, 10];

export default function sum(arr: number[]): number {
  // Retorna un numero que representa la suma de los elementos del arreglo
  // Recomendación: utiliza el método Array.prototype.reduce
  return 0;
}
